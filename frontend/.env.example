# Base URL of the deployed backend, no trailing slash. Only needed for
# production builds (Vercel) — local dev proxies "/api" straight to
# http://localhost:4000 via vite.config.ts, so this can stay unset locally.
VITE_API_URL=
