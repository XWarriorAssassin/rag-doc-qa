export function EmptyState() {
  return (
    <div className="empty-state">
      <div className="empty-state-inner">
        <h3>Select a document</h3>
        <p>Choose a document from the index on the left, or upload a PDF to start asking it questions.</p>
      </div>
    </div>
  );
}
